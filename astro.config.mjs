// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://esherkness14.github.io',
  base: '/runescape-adventure-guide',
  trailingSlash: 'always',
});
